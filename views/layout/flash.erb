<%
  flash_type = session[:flash_type] || "info" #possible types: primary, secondary, success, danger, warning, success, info, light, dark
  session[:flash_type] = nil
  text ||= flash.message
  next unless text.present?
%>

<div class="alert alert-dismissible alert-<%=flash_type%>">
  <button type="button" class="close" data-dismiss="alert">×</button>
  <!-- <h4>Warning!</h4> -->
  <p><%= text %>
    <!-- <a href="javascript:void(0)" class="alert-link">vel scelerisque nisl consectetur et</a>. -->
  </p>
</div>